// OmniRouter — one router for every model.
// main.go is thin on purpose: everything lives in internal/core.
package main

import "github.com/Godde3s/omnirouter/internal/core"

func main() {
	core.Run()
}
